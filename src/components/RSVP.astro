---
// Componente para la confirmación de asistencia
---

<section id="rsvp" class="section">
  <div class="max-w-2xl mx-auto">
    <h2 class="section-title">Confirma tu Asistencia</h2>
    <p class="text-center mb-8">Por favor, confirma tu asistencia antes del 1 de junio de 2025</p>
    
    <form class="space-y-6 bg-white p-8 rounded-lg shadow-md">
      <div class="grid md:grid-cols-2 gap-6">
        <div>
          <label for="name" class="block text-sm font-medium mb-1">Nombre completo</label>
          <input 
            type="text" 
            id="name" 
            name="name" 
            required 
            class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-[#d4b08c]"
          />
        </div>
        <div>
          <label for="email" class="block text-sm font-medium mb-1">Correo electrónico</label>
          <input 
            type="email" 
            id="email" 
            name="email" 
            required 
            class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-[#d4b08c]"
          />
        </div>
      </div>
      
      <div>
        <label for="phone" class="block text-sm font-medium mb-1">Teléfono</label>
        <input 
          type="tel" 
          id="phone" 
          name="phone" 
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-[#d4b08c]"
        />
      </div>
      
      <div>
        <label for="attendance" class="block text-sm font-medium mb-1">¿Asistirás?</label>
        <div class="flex gap-4 mt-2">
          <label class="inline-flex items-center">
            <input type="radio" name="attendance" value="yes" class="mr-2 text-[#d4b08c] focus:ring-[#d4b08c]" checked>
            <span>Sí, asistiré</span>
          </label>
          <label class="inline-flex items-center">
            <input type="radio" name="attendance" value="no" class="mr-2 text-[#d4b08c] focus:ring-[#d4b08c]">
            <span>No podré asistir</span>
          </label>
        </div>
      </div>
      
      <div>
        <label for="guests" class="block text-sm font-medium mb-1">Número de invitados</label>
        <select 
          id="guests" 
          name="guests" 
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-[#d4b08c]"
        >
          <option value="1">1 persona</option>
          <option value="2">2 personas</option>
          <option value="3">3 personas</option>
          <option value="4">4 personas</option>
        </select>
      </div>
      
      <div>
        <label for="message" class="block text-sm font-medium mb-1">Mensaje (opcional)</label>
        <textarea 
          id="message" 
          name="message" 
          rows="3" 
          class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-[#d4b08c]"
        ></textarea>
      </div>
      
      <div class="text-center">
        <button 
          type="submit" 
          class="btn-primary py-3 px-8"
        >
          Confirmar Asistencia
        </button>
      </div>
    </form>
  </div>
</section>

